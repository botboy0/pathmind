---
phase: 01-api-foundation-script-node-registration
plan: 06
type: execute
wave: 2
depends_on: ["01-04"]
gap_closure: true
files_modified:
  - common/src/test/java/com/pathmind/data/AddonNodeConversionRoundTripTest.java
  - common/src/test/java/com/pathmind/data/AddonNodePersistenceTest.java
  - common/src/test/java/com/pathmind/ui/sidebar/AddonSidebarTest.java
  - docs/addon-api-getting-started.md
autonomous: true
requirements: [API-05, API-06, LUA-05]
must_haves:
  truths:
    - "A regression test round-trips an ADDON node through the shared AddonNodeDataCopy helper + NodeGraphPersistence.convertToNodes and asserts addonTypeId is non-null after the trip"
    - "A regression test asserts the editor-load (restoreAddonFieldsToNode) and clipboard copy/restore paths preserve addonTypeId and extraFields"
    - "The getting-started guide pins a Fabric API version that exists for 1.21.4"
  artifacts:
    - path: "common/src/test/java/com/pathmind/data/AddonNodeConversionRoundTripTest.java"
      provides: "Regression coverage for all conversion sites fixed in Plan 01-04"
      contains: "convertToNodes"
    - path: "docs/addon-api-getting-started.md"
      provides: "Corrected Fabric API version"
      contains: "0.119.4+1.21.4"
  key_links:
    - from: "AddonNodeConversionRoundTripTest"
      to: "NodeGraphPersistence.convertToNodes + AddonNodeDataCopy"
      via: "round-trip assertion on addonTypeId"
      pattern: "assert.*getAddonTypeId"
---

<objective>
Close the regression-test gap named in verification: AddonNodePersistenceTest and AddonSidebarTest tested helpers in isolation and missed all three BLOCKER defects because they never exercised the actual conversion sites used by the editor and execution paths. This plan adds an integration-shaped regression test that round-trips an ADDON node through the shared helper + convertToNodes (proving CR-02/CR-03 stay fixed), and folds in two cheap verifier warnings (WR-08 wrong Fabric API version in docs, WR-10 tautological/unused test scaffolding) as secondary cleanup.

Depends on Plan 01-04 because the regression test asserts the behavior of AddonNodeDataCopy and the wired conversion sites.

Purpose: Lock the CR-02/CR-03 fixes under an automated gate so this class of miss cannot silently regress; fix the documented Fabric API version so a third party's first build succeeds (API-10).
Output: New regression test class, cleaned-up existing tests, corrected doc.
</objective>

<artifacts_produced>
## Artifacts this phase produces

New symbols created by this plan (exclude from source-drift verification):
- New test file `common/src/test/java/com/pathmind/data/AddonNodeConversionRoundTripTest.java` and its test methods
- A shared synthetic test registry install (mirrors AddonNodePersistenceTest.installSyntheticRegistry) reused across the round-trip assertions
- Modified existing tests AddonNodePersistenceTest (WR-10 cleanup) and AddonSidebarTest (WR-10 cleanup)
- Edited doc line in docs/addon-api-getting-started.md
</artifacts_produced>

<execution_context>
@C:/Users/Trynda/Desktop/Dev/sidequests/pathmind/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Trynda/Desktop/Dev/sidequests/pathmind/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-api-foundation-script-node-registration/01-VERIFICATION.md
@.planning/phases/01-api-foundation-script-node-registration/01-04-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ADDON conversion round-trip regression test</name>
  <files>common/src/test/java/com/pathmind/data/AddonNodeConversionRoundTripTest.java</files>
  <read_first>
    - common/src/test/java/com/pathmind/data/AddonNodePersistenceTest.java (lines 32-86: TEST_SERIALIZER and installSyntheticRegistry — copy this install pattern; the NodeTypeRegistry is install-once per JVM so the new test must tolerate prior installation by checking hasType first, exactly like lines 66-85)
    - common/src/main/java/com/pathmind/data/AddonNodeDataCopy.java (the helper from Plan 01-04 — copyAddonFieldsToNodeData and restoreAddonFieldsToNode signatures)
    - common/src/main/java/com/pathmind/data/NodeGraphPersistence.java (line 253: `public static List<Node> convertToNodes(NodeGraphData data)` — the load entry point the test drives; lines 339-368 for the ADDON branch behavior)
    - common/src/main/java/com/pathmind/data/NodeGraphData.java (NodeData constructor, setId/setType/setAddonTypeId/setExtraFields)
    - common/src/main/java/com/pathmind/nodes/Node.java (Node(String addonTypeId, int x, int y) constructor, getAddonTypeId, getAddonExtraFields, setAddonExtraFields)
    - common/src/main/java/com/pathmind/nodes/NodeType.java (ADDON)
  </read_first>
  <behavior>
    - Test 1 (snapshot→convert, CR-03): build a live Node via new Node("test_mod:script", x, y), set addonExtraFields containing "script" text; build a NodeData, call AddonNodeDataCopy.copyAddonFieldsToNodeData(node, nodeData); set its id/type; wrap in a NodeGraphData; call NodeGraphPersistence.convertToNodes(data); assert the restored node's getAddonTypeId() equals "test_mod:script" (non-null) and the script text survives in getAddonExtraFields().
    - Test 2 (editor-load restore, CR-02): build a NodeData with type ADDON, addonTypeId set, extraFields containing _schema_version + script; construct a fresh Node via new Node(NodeType.ADDON, x, y); call AddonNodeDataCopy.restoreAddonFieldsToNode(nodeData, node); assert getAddonTypeId() is non-null and the "script" key is present in getAddonExtraFields().
    - Test 3 (clipboard copy then restore, CR-02): copyAddonFieldsToNodeData from a live node into a NodeData, then restoreAddonFieldsToNode into a new node; assert addonTypeId and script survive the round-trip and that the restored extraFields is a distinct map instance (deep copy, not aliased).
    - Test 4 (missing-addon placeholder, D-09): with the synthetic registry NOT containing a given addonTypeId, restoreAddonFieldsToNode retains extraFields verbatim and marks the node addonUnresolved (assert isAddonUnresolved() true and extraFields preserved).
  </behavior>
  <action>
    Create AddonNodeConversionRoundTripTest in package com.pathmind.data using JUnit 5 (org.junit.jupiter.api). Reuse the synthetic registry install approach from AddonNodePersistenceTest (install-once guard via NodeTypeRegistry.INSTANCE.hasType before installing; register test_mod:script with a serializer that round-trips _schema_version=1 + "script"). Implement the four tests described in <behavior> using assertNotNull/assertEquals/assertTrue/assertNotSame from org.junit.jupiter.api.Assertions (no bare `assert` — see IN-01). For Test 4, use an addonTypeId that is deliberately NOT registered so the missing-addon branch runs.

    Because NodeTypeRegistry is a JVM-wide install-once singleton and AddonNodePersistenceTest also installs test_mod:script, the install must be tolerant: guard with hasType and catch IllegalStateException exactly as the existing test does. The registered serializer's deserialize must set ctx scriptText from the "script" key using ((Number)).intValue() for the version (Pitfall 4 GSON Double erasure).

    This is the regression test the verifier said was missing — it exercises the real conversion path (convertToNodes + the shared helper), not the serializer in isolation.
  </action>
  <verify>
    <automated>cd C:/Users/Trynda/Desktop/Dev/sidequests/pathmind && ./gradlew.bat :common:test --tests "com.pathmind.data.AddonNodeConversionRoundTripTest" -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File common/src/test/java/com/pathmind/data/AddonNodeConversionRoundTripTest.java exists
    - It calls NodeGraphPersistence.convertToNodes( and AddonNodeDataCopy.copyAddonFieldsToNodeData( and AddonNodeDataCopy.restoreAddonFieldsToNode(
    - It contains an assertion of the form assert*(...getAddonTypeId()...) checking non-null after the round-trip (the CR-03 proof)
    - It contains an assertNotSame (or equivalent identity check) proving extraFields is deep-copied, and an isAddonUnresolved() assertion for the missing-addon case
    - Uses JUnit assertions, not bare `assert`
    - `./gradlew.bat :common:test --tests "com.pathmind.data.AddonNodeConversionRoundTripTest"` exits 0 (all four tests pass against the Plan 01-04 fixes)
  </acceptance_criteria>
  <done>An automated test fails if any of the three conversion sites regresses to dropping addonTypeId/extraFields; it passes against the Plan 01-04 wiring.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix WR-08 doc version and WR-10 test scaffolding</name>
  <files>docs/addon-api-getting-started.md, common/src/test/java/com/pathmind/data/AddonNodePersistenceTest.java, common/src/test/java/com/pathmind/ui/sidebar/AddonSidebarTest.java</files>
  <read_first>
    - docs/addon-api-getting-started.md (line 93: the modImplementation fabric-api coordinate — change 0.102.0+1.21.4 to 0.119.4+1.21.4; cross-check against the sibling repo's correct value at C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/gradle.properties:15)
    - common/src/test/java/com/pathmind/data/AddonNodePersistenceTest.java (lines 65-86: installSyntheticRegistry @BeforeAll that installs a global singleton no test in the class uses — WR-10b)
    - common/src/test/java/com/pathmind/ui/sidebar/AddonSidebarTest.java (lines 135-148: getHoveredAddonDefinition_returnsNullByDefault ends in assertNull(null,...) — tautological, WR-10a)
  </read_first>
  <action>
    WR-08: In docs/addon-api-getting-started.md line 93, change the Fabric API version from 0.102.0+1.21.4 to 0.119.4+1.21.4 (the version that actually exists for 1.21.4 and matches the sibling repo). If the doc's gradle.properties example block omits fabric_api_version, add it there too so the template is internally consistent.

    WR-10a: In AddonSidebarTest, replace the tautological getHoveredAddonDefinition_returnsNullByDefault test. Since Plan 01-05 now wires hoveredAddonDefinition, either delete this test (the real hover behavior is render-loop wiring that requires a Minecraft client harness and is out of unit-test scope) OR convert it to a meaningful assertion against groupByCategory/Sidebar's public accessor that does not require a live client. Do not leave assertNull(null, ...).

    WR-10b: In AddonNodePersistenceTest, the @BeforeAll installSyntheticRegistry consumes the JVM-wide install-once singleton but no test in the class uses NodeTypeRegistry.INSTANCE (they call TEST_SERIALIZER directly). Coordinate with Task 1: the new AddonNodeConversionRoundTripTest DOES need test_mod:script installed in INSTANCE. Keep exactly ONE install of test_mod:script across the suite that both classes tolerate via the hasType guard (the guard already handles cross-class ordering). Remove any dead/unused install or document why it must stay. Do not break either test class — run both to confirm.
  </action>
  <verify>
    <automated>cd C:/Users/Trynda/Desktop/Dev/sidequests/pathmind && ./gradlew.bat :common:test --tests "com.pathmind.data.*" --tests "com.pathmind.ui.sidebar.AddonSidebarTest" -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "0.102.0+1.21.4" docs/addon-api-getting-started.md` returns no matches
    - `grep -n "0.119.4+1.21.4" docs/addon-api-getting-started.md` returns at least one match
    - AddonSidebarTest no longer contains the literal `assertNull(null` (the tautology is gone)
    - The full :common test run for com.pathmind.data.* and AddonSidebarTest exits 0 (no install-once ordering breakage between AddonNodePersistenceTest and AddonNodeConversionRoundTripTest)
  </acceptance_criteria>
  <done>The getting-started guide builds for a third party (correct Fabric API version), and the test suite no longer contains a tautological assertion or a dead global-singleton install that breaks the new round-trip test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds test code and a documentation string edit only — no runtime code path, no input parsing, no persistence/execution surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | Tampering | Test fixtures installing into the global NodeTypeRegistry singleton | accept | The install-once guard (hasType + catch IllegalStateException) prevents cross-class ordering failures; this is a test-only concern with no production impact. |

No high-severity (ASVS L1 blocking) threats apply: this plan introduces no new production code, no new dependencies (the Fabric API version change corrects an existing coordinate to a real published artifact — it does not add a package, so no Package Legitimacy Gate is triggered), and no trust boundary is crossed. Explicitly: no install task for a new package, no deserialization, no network/filesystem write beyond the test JVM.
</threat_model>

<verification>
- `./gradlew.bat :common:test --tests "com.pathmind.data.AddonNodeConversionRoundTripTest"` exits 0 — the regression gate passes against the Plan 01-04 fixes.
- Running both AddonNodePersistenceTest and AddonNodeConversionRoundTripTest in the same JVM does not throw an install-once IllegalStateException (the hasType guard holds).
- docs/addon-api-getting-started.md no longer references 0.102.0+1.21.4.
- AddonSidebarTest contains no `assertNull(null` tautology.
</verification>

<success_criteria>
- Regression gap closed: an automated test asserts addonTypeId survives the createGraphSnapshot/convertToNodes and editor-load/clipboard round-trips — it would have caught CR-02 and CR-03.
- WR-08 closed: API-10 getting-started guide pins a Fabric API version that resolves for 1.21.4.
- WR-10 closed: no tautological test, no dead singleton install conflicting with the new test.
</success_criteria>

<output>
Create `.planning/phases/01-api-foundation-script-node-registration/01-06-SUMMARY.md` when done.
</output>
