---
phase: 01-api-foundation-script-node-registration
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - fabric/build.gradle.kts
  - docs/addon-api-getting-started.md
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/settings.gradle.kts
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/build.gradle.kts
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/gradle.properties
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/.gitignore
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/main/resources/fabric.mod.json
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/main/java/com/mrmysterium/pathmindlua/LuaAddonEntrypoint.java
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/main/java/com/mrmysterium/pathmindlua/LuaNodeExecutor.java
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/main/java/com/mrmysterium/pathmindlua/LuaNodeSerializer.java
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/main/java/com/mrmysterium/pathmindlua/LuaScriptNodeRenderer.java
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/docs/dev-loop.md
autonomous: true
user_setup: []
requirements: [API-08, API-10, LUA-01, LUA-05]
must_haves:
  truths:
    - "Pathmind publishes com.pathmind:pathmind-fabric to mavenLocal via ./gradlew :fabric:publishToMavenLocal"
    - "The sibling pathmind-lua repo exists as a standalone Gradle/Loom project that compiles against the Pathmind artifact via modCompileOnly with zero impl classes on its classpath"
    - "pathmind-lua's fabric.mod.json declares the \"pathmind\" entrypoint pointing at LuaAddonEntrypoint"
    - "LuaAddonEntrypoint registers a Lua Script node (category Scripting, no-op executor, script+_schema_version serializer, read-only preview renderer)"
    - "The addon api getting-started doc exists and is sufficient for a third party to build a different addon"
  artifacts:
    - path: "fabric/build.gradle.kts"
      provides: "maven-publish plugin + publication of pathmind-fabric to mavenLocal"
      contains: "maven-publish"
    - path: "C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/main/resources/fabric.mod.json"
      provides: "Addon mod metadata + pathmind entrypoint declaration"
      contains: "\"pathmind\""
    - path: "C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/main/java/com/mrmysterium/pathmindlua/LuaAddonEntrypoint.java"
      provides: "Addon entrypoint registering the Lua Script node"
      contains: "implements PathmindAddonEntrypoint"
    - path: "docs/addon-api-getting-started.md"
      provides: "Third-party addon getting-started guide (API-10)"
      contains: "modCompileOnly"
  key_links:
    - from: "C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/build.gradle.kts"
      to: "com.pathmind:pathmind-fabric"
      via: "modCompileOnly against mavenLocal"
      pattern: "modCompileOnly.*pathmind"
    - from: "C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/main/resources/fabric.mod.json"
      to: "com.mrmysterium.pathmindlua.LuaAddonEntrypoint"
      via: "pathmind entrypoint key"
      pattern: "LuaAddonEntrypoint"
---

<objective>
Prove the API is a real, consumable external artifact: add `maven-publish` to Pathmind's fabric build so `com.pathmind:pathmind-fabric` lands in mavenLocal, scaffold the sibling `pathmind-lua` repo as a standalone Loom project that compiles against that artifact via `modCompileOnly` (zero impl classes on its classpath), implement the Lua Script node (entrypoint + no-op executor + script/_schema_version serializer + read-only preview renderer), and write the addon API getting-started guide. After this plan, a third party's `pathmind-lua-x.y.z.jar` dropped next to `pathmind.jar` registers a working (no-op) Script node — the phase's core value proposition.

Purpose: This is the external-consumer vertical slice. It depends only on the API package (Plan 01) — it does NOT depend on Plan 02's in-Pathmind wiring to compile (the addon implements the published API interfaces). It runs in parallel with Plan 02 in Wave 2. The end-to-end in-game proof (both jars loaded) is the end-of-phase human verification folded into the final task.

Output: maven-publish wired into Pathmind; a complete sibling repo at C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua; four addon Java classes; addon getting-started doc + dev-loop doc.
</objective>

<execution_context>
@C:/Users/Trynda/Desktop/Dev/sidequests/pathmind/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Trynda/Desktop/Dev/sidequests/pathmind/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-api-foundation-script-node-registration/01-CONTEXT.md
@.planning/phases/01-api-foundation-script-node-registration/01-RESEARCH.md
@.planning/phases/01-api-foundation-script-node-registration/01-PATTERNS.md
@.planning/phases/01-api-foundation-script-node-registration/01-01-SUMMARY.md
</context>

## Phase Goal

**As a** third-party developer, **I want to** add `com.pathmind:pathmind-fabric` to my addon's build via `modCompileOnly` and ship a jar that drops next to Pathmind and registers a Script node, **so that** I have proof the Pathmind addon API is real, stable, and consumable from an external repo without any Pathmind impl classes leaking into my project.

## Artifacts this phase produces

New symbols / files created in this plan (exclude from drift/source-grounding checks):

- Pathmind `fabric/build.gradle.kts` `maven-publish` plugin + `publishing { publications { create<MavenPublication>("mavenFabric") { artifactId = "pathmind-fabric" ... } } }`
- Entire new repo at `C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/` (settings.gradle.kts, build.gradle.kts, gradle.properties, .gitignore, fabric.mod.json)
- `com.mrmysterium.pathmindlua.LuaAddonEntrypoint` (implements `PathmindAddonEntrypoint`)
- `com.mrmysterium.pathmindlua.LuaNodeExecutor` (implements `AddonNodeExecutor` — Phase 1 no-op)
- `com.mrmysterium.pathmindlua.LuaNodeSerializer` (implements `AddonNodeSerializer`)
- `com.mrmysterium.pathmindlua.LuaScriptNodeRenderer` (implements `AddonNodeBodyRenderer`)
- `docs/addon-api-getting-started.md` (Pathmind repo)
- `pathmind-lua/docs/dev-loop.md` (cache-busting publish loop)

<tasks>

<task type="auto">
  <name>Task 1: Publish the Pathmind API artifact to mavenLocal + write the getting-started doc</name>
  <files>
    fabric/build.gradle.kts,
    docs/addon-api-getting-started.md
  </files>
  <read_first>
    - fabric/build.gradle.kts (lines 1-10 plugins block; whole file to find where to append publishing block)
    - gradle.properties (mod_version=1.1.5, maven_group=com.pathmind — the publication coordinates)
    - .planning/phases/01-api-foundation-script-node-registration/01-RESEARCH.md (Standard Stack install snippet; Pitfall 2 impl-classes-in-artifact; A2 remapJar auto-wire assumption)
    - .planning/phases/01-api-foundation-script-node-registration/01-PATTERNS.md (fabric/build.gradle.kts modification skeleton)
    - common/src/main/java/com/pathmind/api/addon/PathmindAddonEntrypoint.java (from Plan 01 — referenced by the doc)
  </read_first>
  <action>
    In `fabric/build.gradle.kts`: add `id("maven-publish")` to the plugins block (alongside the existing dev.architectury.loom / architectury-plugin / com.gradleup.shadow ids). Append a `publishing` block at end of file creating a `MavenPublication` named "mavenFabric" with groupId `com.pathmind`, artifactId `pathmind-fabric`, and `from(components["java"])`. The version is inherited from the project version (mod_version=1.1.5). Per RESEARCH.md A2, Loom auto-wires `remapJar` as the publication artifact — the verify step confirms the artifact installed under ~/.m2. Per Pitfall 2 (Option 2, package discipline), do NOT attempt a filtered api-classifier jar in Phase 1 — publish the full remapped jar and rely on documented package discipline plus the addon's clean compile (Task 2/3) to prove no impl import is needed. Add an inline comment in build.gradle.kts noting the dev-loop command `./gradlew :fabric:publishToMavenLocal` and the Loom cache-bust requirement (Pitfall 1).

    Run `./gradlew :fabric:publishToMavenLocal` and confirm `com.pathmind:pathmind-fabric:1.1.5` appears under `~/.m2/repository/com/pathmind/pathmind-fabric/1.1.5/`.

    Write `docs/addon-api-getting-started.md` (API-10): cover (1) declaring the `"pathmind"` entrypoint in fabric.mod.json pointing at a class implementing `PathmindAddonEntrypoint`; (2) adding `mavenLocal()` and `modCompileOnly("com.pathmind:pathmind-fabric:<version>")` to the addon build; (3) implementing `registerNodes(NodeTypeRegistrar)` — register an AddonNodeDefinition (built via the builder), an AddonNodeExecutor (returns CompletableFuture<NodeResult>), and an AddonNodeSerializer (must write "_schema_version"); (4) declaring an AddonNodeCategory; (5) the node-id format rule `^[a-z0-9_-]+:[a-z0-9_/.-]+$`; (6) the GSON Double-erasure caveat for serializers (read numbers via ((Number)x).intValue()); (7) the dev-loop cache-bust note. Keep it concrete enough that a developer building a NON-Lua addon could follow it. Reference API-08 and API-10 in the commit message.
  </action>
  <verify>
    <automated>cd C:/Users/Trynda/Desktop/Dev/sidequests/pathmind && ./gradlew :fabric:publishToMavenLocal -q 2>&1 | tail -15; ls ~/.m2/repository/com/pathmind/pathmind-fabric/ 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - fabric/build.gradle.kts contains `id("maven-publish")` and a `create<MavenPublication>("mavenFabric")` block with `artifactId = "pathmind-fabric"`
    - `./gradlew :fabric:publishToMavenLocal` exits 0
    - A jar exists under `~/.m2/repository/com/pathmind/pathmind-fabric/1.1.5/` (ls returns the version directory with a .jar)
    - docs/addon-api-getting-started.md contains `modCompileOnly` and the entrypoint key `"pathmind"` and the node-id regex
  </acceptance_criteria>
  <done>The Pathmind API artifact is published to mavenLocal and the getting-started guide documents the full addon-authoring path for any third party.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the pathmind-lua sibling repo (Gradle/Loom project + fabric.mod.json + dev-loop doc)</name>
  <files>
    C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/settings.gradle.kts,
    C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/build.gradle.kts,
    C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/gradle.properties,
    C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/.gitignore,
    C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/main/resources/fabric.mod.json,
    C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/docs/dev-loop.md
  </files>
  <read_first>
    - fabric/build.gradle.kts (Pathmind's fabric build — Loom version, repositories, mc/yarn/loader/fabric-api coordinates to mirror for 1.21.4)
    - fabric/src/main/resources/fabric.mod.json (Pathmind's mod metadata structure to mirror; entrypoints block format)
    - gradle.properties (Pathmind versions for reference)
    - .planning/phases/01-api-foundation-script-node-registration/01-PATTERNS.md (pathmind-lua/build.gradle.kts and fabric.mod.json copy-ready skeletons)
    - .planning/phases/01-api-foundation-script-node-registration/01-RESEARCH.md (Supporting stack table; Pitfall 1 mavenLocal cache-bust)
  </read_first>
  <action>
    Create the standalone `pathmind-lua` repo at `C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/` (D-02 location). Target Minecraft 1.21.4 only (addon v1 single-version constraint from CLAUDE.md / D-03):
    - `settings.gradle.kts`: pluginManagement repositories (architectury, fabric, gradlePluginPortal) and `rootProject.name = "pathmind-lua"`.
    - `gradle.properties`: `maven_group=com.mrmysterium`, `mod_version=0.1.0`, `minecraft_version=1.21.4`, `loader_version=0.17.3`, `pathmind_version=1.1.5`, plus yarn/fabric-api versions for 1.21.4 (yarn `1.21.4+build.8`, and the fabric-api version matching CLAUDE.md's 1.21.4 row — verify the exact 1.21.4 fabric-api version against Pathmind's gradle before pinning). Java 21 source/target.
    - `build.gradle.kts`: plugins `dev.architectury.loom` 1.14.473 + `architectury-plugin` 3.4.161 (matching Pathmind's Loom toolchain per D-03 constraint). repositories include `mavenLocal()` (FIRST, so the Pathmind artifact resolves), architectury maven, fabric maven. dependencies: minecraft 1.21.4, yarn mappings, `modImplementation` fabric-loader + fabric-api, and the key line `modCompileOnly("com.pathmind:pathmind-fabric:${property("pathmind_version")}")`. Set Java toolchain to 21.
    - `.gitignore`: standard Gradle/Loom ignores (`.gradle/`, `build/`, `run/`, `*.class`, IDE files).
    - `src/main/resources/fabric.mod.json`: id `pathmind-lua`, name "Pathmind Lua", authors `["mr_mysterium"]` (D-03), environment `client`, entrypoints block with key `"pathmind": ["com.mrmysterium.pathmindlua.LuaAddonEntrypoint"]` (D-04), depends `fabricloader >=0.17.3`, `minecraft "1.21.4"`, `java >=21`, and `pathmind ">=0.1.0"` (D-11 declared API-version dependency — the Fabric loader half of the two-layer compatibility check).
    - `docs/dev-loop.md`: document the publish-and-cache-bust loop (Pitfall 1): `cd ../pathmind && ./gradlew :fabric:publishToMavenLocal` then `rm -rf .gradle/loom-cache/remapped_mods` in pathmind-lua, then rebuild. Explain why (Loom 1.14 caches by version coordinate, not content hash — issue #1290).
    Initialize the repo as a git repository (git init) but do NOT commit — leave it for the developer. The Gradle wrapper can be copied from Pathmind (copy `gradle/wrapper/` and `gradlew`/`gradlew.bat` from the pathmind repo). If copying the wrapper is impractical in the sandbox, document the `gradle wrapper --gradle-version 8.x` bootstrap step in dev-loop.md instead. Reference D-01, D-02, D-03, D-04, D-11 in any commit you make inside the pathmind repo for the docs; the sibling repo stays uncommitted.
  </action>
  <verify>
    <automated>ls C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/build.gradle.kts C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/main/resources/fabric.mod.json C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/gradle.properties 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - The three files above exist (ls returns all three, no "No such file")
    - pathmind-lua/build.gradle.kts contains `modCompileOnly` referencing `com.pathmind:pathmind-fabric` and lists `mavenLocal()` in repositories
    - pathmind-lua/src/main/resources/fabric.mod.json contains the entrypoint key `"pathmind"` mapping to `com.mrmysterium.pathmindlua.LuaAddonEntrypoint` and a `pathmind` dependency entry
    - pathmind-lua/gradle.properties contains `maven_group=com.mrmysterium` and `minecraft_version=1.21.4`
    - pathmind-lua/docs/dev-loop.md contains `publishToMavenLocal` and `remapped_mods`
  </acceptance_criteria>
  <done>A standalone, Loom-based pathmind-lua repo exists that targets 1.21.4, resolves the Pathmind API from mavenLocal via modCompileOnly, declares the pathmind entrypoint, and documents the dev cache-bust loop.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the Lua Script node (entrypoint, no-op executor, serializer, preview renderer) + end-of-phase in-game verification</name>
  <files>
    C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/main/java/com/mrmysterium/pathmindlua/LuaAddonEntrypoint.java,
    C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/main/java/com/mrmysterium/pathmindlua/LuaNodeExecutor.java,
    C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/main/java/com/mrmysterium/pathmindlua/LuaNodeSerializer.java,
    C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/main/java/com/mrmysterium/pathmindlua/LuaScriptNodeRenderer.java
  </files>
  <read_first>
    - .planning/phases/01-api-foundation-script-node-registration/01-PATTERNS.md (LuaAddonEntrypoint / LuaNodeExecutor / LuaNodeSerializer / LuaScriptNodeRenderer copy-ready skeletons)
    - common/src/main/java/com/pathmind/api/addon/PathmindAddonEntrypoint.java (the interface to implement)
    - common/src/main/java/com/pathmind/api/addon/AddonNodeDefinition.java (builder API + AddonNodeCategory + AddonNodeBodyRenderer signatures from Plan 01)
    - common/src/main/java/com/pathmind/api/addon/AddonNodeExecutor.java (execute returns CompletableFuture<NodeResult>)
    - common/src/main/java/com/pathmind/api/addon/AddonNodeSerializer.java (serialize/deserialize contract + _schema_version rule)
  </read_first>
  <action>
    Implement the four addon classes under `com.mrmysterium.pathmindlua` (package layout is Claude's discretion per CONTEXT.md), importing ONLY from `com.pathmind.api.addon.*` and `com.pathmind.api.*` (plus Minecraft `DrawContext`/`MinecraftClient` for the renderer). NEVER import `com.pathmind.execution.*`, `com.pathmind.ui.*`, `com.pathmind.nodes.Node`, `com.pathmind.data.*` — if any such import is needed to compile, the API surface is broken and must be reported, not worked around (RESEARCH.md anti-pattern + Pitfall 2 — this clean compile is the API-08 proof).
    - `LuaAddonEntrypoint implements PathmindAddonEntrypoint`: in `registerNodes(NodeTypeRegistrar registrar)`, build an `AddonNodeCategory("pathmind_lua.scripting", "Scripting", 0xFF7986CB, "*")` (D-05 own category) and call `registrar.register(AddonNodeDefinition.builder("pathmind_lua:script").displayName("Lua Script").category(...).color(0xFF7986CB).provenanceLabel("Pathmind Lua").bodyRenderer(new LuaScriptNodeRenderer()).build(), new LuaNodeExecutor(), new LuaNodeSerializer())` (D-07 provenance label). Use an ASCII-safe icon string (avoid non-ASCII glyphs that may not render).
    - `LuaNodeExecutor implements AddonNodeExecutor`: `execute(ctx)` returns `CompletableFuture.completedFuture(NodeResult.SUCCESS)` with a comment that Phase 2 replaces this with Cobalt VM execution (graceful no-op pass-through, LUA-01 success criterion #3).
    - `LuaNodeSerializer implements AddonNodeSerializer`: `serialize` returns a LinkedHashMap with `"_schema_version" -> 1` and `"script" -> ctx.getScriptText()` (default to a placeholder script when null). `deserialize` reads `"script"` as String into the ctx; tolerate a null fields map by setting the default script. Do NOT cast `_schema_version` with `(Integer)` — if read at all, use `((Number) f.get("_schema_version")).intValue()` (Pitfall 4). This is the LUA-05 script-persistence implementation.
    - `LuaScriptNodeRenderer implements AddonNodeBodyRenderer`: `render(ctx, draw, x, y, width, height)` draws the first up-to-3 lines of `ctx.getScriptText()` via `MinecraftClient.getInstance().textRenderer` and `draw.drawTextWithShadow(...)`, truncating each line with an ellipsis when wider than `width` (D-06 read-only preview). Return early if script is null/blank.
    Build the addon: run `./gradlew build` (or `gradle build`) in pathmind-lua to prove it compiles against ONLY the published API artifact. Reference D-05, D-06, D-07, LUA-01, LUA-05 in any docs commit.
  </action>
  <verify>
    <automated>cd C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua && (./gradlew build -q 2>&1 | tail -25 || gradle build -q 2>&1 | tail -25); echo "---IMPL-LEAK-CHECK---"; grep -rn "com.pathmind.execution\|com.pathmind.ui\|com.pathmind.nodes.Node\|com.pathmind.data" src/main/java && echo "IMPL LEAK FOUND" || echo "NO IMPL LEAK"</automated>
    <human-check>
      End-of-phase in-game verification (human_verify_mode=end-of-phase). Run AFTER Plan 02 is also complete, since the in-Pathmind wiring (sidebar, execution, persistence) lives in Plan 02.

      Part A — both jars (addon present):
      1. Build the Pathmind fabric jar (`./gradlew :fabric:remapJar` or `build`) and the pathmind-lua jar (`./gradlew build` in pathmind-lua), both for MC 1.21.4.
      2. Put `pathmind-*-mc1.21.4.jar` and `pathmind-lua-0.1.0.jar` (+ Fabric API + Architectury) in a 1.21.4 Fabric mods folder. Launch.
      3. Confirm the log shows `Addon 'pathmind-lua' registered nodes successfully` and no errors.
      4. Open the Pathmind editor. Confirm a "Scripting" category appears with a "Lua Script" node carrying the provenance marker (accent color / "Pathmind Lua" label).
      5. Drag the Lua Script node onto the canvas, connect it after a START node, and run the graph. Confirm it passes through (graceful no-op) with no error and the graph advances.
      6. Save the preset, then reload it. Confirm the Script node returns with its script text intact and the preset JSON contains `addonTypeId` and an `extraFields` object with `"_schema_version": 1` and `"script"`.
      7. Remove the pathmind-lua jar, relaunch with only Pathmind, load the same preset. Confirm the Script node loads as a grayed-out placeholder that preserves its data (re-saving keeps the blob), and the editor surfaces the addon-missing/failure handling gracefully.

      Part B — standalone Pathmind (API-09, no addon ever installed):
      8. With ONLY pathmind.jar (no addon, fresh preset folder), launch on 1.21.4 and on at least one other supported version (e.g. 1.21.11). Confirm no errors, no "Scripting" category, and Pathmind behaves exactly as before — built-in nodes place, run, save, and load normally.
    </human-check>
  </verify>
  <acceptance_criteria>
    - LuaAddonEntrypoint.java contains `implements PathmindAddonEntrypoint` and `registrar.register(`
    - LuaNodeExecutor.java contains `CompletableFuture.completedFuture(NodeResult.SUCCESS)`
    - LuaNodeSerializer.java contains `"_schema_version"` and writes `"script"`; no `(Integer)` cast of the schema version
    - grep for `com.pathmind.execution`/`com.pathmind.ui`/`com.pathmind.nodes.Node`/`com.pathmind.data` in pathmind-lua/src/main/java returns NO matches (prints "NO IMPL LEAK") — this is the API-08 zero-impl-classes proof
    - `./gradlew build` (or `gradle build`) in pathmind-lua exits 0 — addon compiles + jars against only the API artifact
    - Human-check Parts A and B pass (addon Script node visible/placeable/runnable/persistent with addon present; lossless grayed placeholder with addon absent; Pathmind fully functional standalone across versions)
  </acceptance_criteria>
  <done>The Lua Script node is fully implemented in the sibling repo, compiles against only the published API artifact with zero impl-class imports, produces pathmind-lua-0.1.0.jar, and the in-game end-of-phase check confirms the full drop-two-jars-get-a-Script-node experience plus standalone Pathmind integrity.</done>
</task>

</tasks>

<verification>
- `./gradlew :fabric:publishToMavenLocal` installs `com.pathmind:pathmind-fabric:1.1.5` under ~/.m2
- pathmind-lua compiles + jars against only the API artifact; grep confirms zero impl-class imports
- fabric.mod.json declares the "pathmind" entrypoint at LuaAddonEntrypoint
- Human in-game check confirms: addon Script node visible/placeable/runnable/persistent with addon present; lossless grayed placeholder with addon absent; Pathmind fully functional standalone across versions
</verification>

<success_criteria>
- API-08: separate API artifact published to local Maven; sibling repo compiles with zero impl classes on classpath
- API-10: addon API getting-started guide written; sufficient for a third party to build a different addon
- LUA-01: user can grab the Script node from the palette and place it (verified in-game)
- LUA-05: script text persists through preset save/load with _schema_version (verified in-game + by Plan 02's automated round-trip test)
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published Maven artifact → addon classpath | The addon compiles against the published jar; if impl classes leak into the artifact, the API boundary is broken from day one |
| addon jar → Pathmind at runtime | The addon's entrypoint runs inside Pathmind's init via the discovery wired in Plan 01 |
| mavenLocal cache → addon build | A stale cached remapped jar can silently compile the addon against the wrong API version |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Information Disclosure | Published artifact leaking impl classes (addon imports com.pathmind.execution.*) | mitigate | Package-discipline check: Task 3 greps the addon source for impl-package imports and the addon must compile with only com.pathmind.api on the classpath (Pitfall 2 Option 2). A leak fails the verify step |
| T-01-11 | Tampering | Stale mavenLocal remapped jar → addon built against wrong API version (NoSuchMethodError at runtime) | mitigate | dev-loop.md documents the publish + `rm -rf .gradle/loom-cache/remapped_mods` cache-bust loop (Pitfall 1 / Loom #1290) |
| T-01-12 | Spoofing | Addon declares incompatible API version | mitigate | D-11 two-layer check: fabric.mod.json `pathmind ">=0.1.0"` dependency lets the loader block hard mismatches; runtime registration check (Plan 01 registry) disables incompatible addons via the standard failure UX |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs. The only new dependency is `modCompileOnly` against Pathmind itself (first-party). Phase 2 introduces Cobalt and runs the package-legitimacy gate then (RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<output>
Create `.planning/phases/01-api-foundation-script-node-registration/01-03-SUMMARY.md` when done
</output>
