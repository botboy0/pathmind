---
phase: 01-api-foundation-script-node-registration
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - common/src/main/java/com/pathmind/ui/sidebar/Sidebar.java
autonomous: true
requirements: [LUA-01, API-07]
must_haves:
  truths:
    - "When an addon is installed, an addon-declared category (e.g. 'Scripting') appears in the sidebar with its registered node entries"
    - "Hovering an addon node entry sets hoveredAddonDefinition non-null, enabling drag-to-canvas via the existing createNodeFromSidebar addon branch"
    - "Clicking/dragging an addon entry starts a drag (mouseClicked drag-start guard recognizes hoveredAddonDefinition)"
  artifacts:
    - path: "common/src/main/java/com/pathmind/ui/sidebar/Sidebar.java"
      provides: "Addon category render + hit-test loop that consumes addonCategoryNodes and assigns hoveredAddonDefinition"
      contains: "hoveredAddonDefinition ="
  key_links:
    - from: "Sidebar render/hit-test loop"
      to: "addonCategoryNodes"
      via: "iteration that draws category headers + entry rows"
      pattern: "for.*addonCategoryNodes"
    - from: "Sidebar hit-test"
      to: "hoveredAddonDefinition"
      via: "non-null assignment when an addon entry is hovered"
      pattern: "hoveredAddonDefinition\\s*=\\s*[a-z]"
    - from: "Sidebar.mouseClicked drag-start guard"
      to: "createNodeFromSidebar addon branch"
      via: "hoveredAddonDefinition included in the drag-start condition"
      pattern: "hoveredAddonDefinition != null"
---

<objective>
Close CR-01 (BLOCKER): the addon sidebar palette is structurally dead. `Sidebar.addonCategoryNodes` is populated by `initializeAddonCategoryNodes()` but no render or hit-test code ever iterates it, and `hoveredAddonDefinition` is only ever assigned null (line 71 init, line 129 clear). The `createNodeFromSidebar` addon branch (line 1032) and the drag/drop Node-overloads are already in place but unreachable because nothing sets the hover state.

This plan adds the missing render + hit-test wiring so addon-declared categories appear in the sidebar, hovering an addon entry sets `hoveredAddonDefinition`, and the existing drag-to-canvas path becomes reachable. This delivers D-05 (addon-declared categories) and unblocks LUA-01 (Script node placeable from palette) and makes API-07's renderer reachable for placed nodes.

Purpose: Restore verification truth #3 (drag a Script node from the palette) at the placement layer.
Output: Addon category tabs/section rendering + hit-test in Sidebar.java.
</objective>

<artifacts_produced>
## Artifacts this phase produces

New symbols/behaviors created by this plan (exclude from source-drift verification):
- New render block in `Sidebar.render(...)` that iterates `addonCategoryNodes` to draw an addon category section (header from AddonNodeCategory.getDisplayName()/getColor()/getIcon(), entry rows from AddonNodeDefinition.getDisplayName())
- New hit-test logic in `Sidebar.render(...)` that assigns `hoveredAddonDefinition = def` when an addon entry row is hovered and clears it otherwise
- Possible new private helper(s) on Sidebar for addon-category layout (e.g. a row-height/tab bookkeeping field for the addon section) and an addon-category selection field if the implementation mirrors the NodeCategory tab/selected-category model
- Updated `Sidebar.mouseClicked` drag-start guard and category-switch hover reset to account for `hoveredAddonDefinition`
- Updated mouse-leaves-sidebar hover reset (line ~951-955) to clear `hoveredAddonDefinition`
</artifacts_produced>

<execution_context>
@C:/Users/Trynda/Desktop/Dev/sidequests/pathmind/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Trynda/Desktop/Dev/sidequests/pathmind/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-api-foundation-script-node-registration/01-VERIFICATION.md
@.planning/phases/01-api-foundation-script-node-registration/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Render addon categories and set hoveredAddonDefinition in the hit-test loop</name>
  <files>common/src/main/java/com/pathmind/ui/sidebar/Sidebar.java</files>
  <read_first>
    - common/src/main/java/com/pathmind/ui/sidebar/Sidebar.java (lines 64-71: hoveredNodeType + addonCategoryNodes + hoveredAddonDefinition fields; lines 127-165: initializeAddonCategoryNodes, groupByCategory, getAddonCategoryNodesForTest, getHoveredAddonDefinition accessors; lines 586-718: NodeCategory tab rendering loop — the model to mirror for addon category tabs; lines 720-912: the selected-category content render + per-node hit-test where hoveredNodeType is set at 831/879 — this is the exact pattern to mirror for hoveredAddonDefinition; lines 938-955: tooltip + mouse-leave hover reset; lines 958-1021: mouseClicked including the drag-start guard at 988 and the category-switch hover reset at 980; lines 1024-1041: isHoveringNode and createNodeFromSidebar addon branch that consumes hoveredAddonDefinition)
    - common/src/main/java/com/pathmind/api/addon/AddonNodeCategory.java (getDisplayName, getColor, getIcon)
    - common/src/main/java/com/pathmind/api/addon/AddonNodeDefinition.java (getId, getDisplayName, getColor)
  </read_first>
  <action>
    Make the addon categories appear in the sidebar and become hoverable. Mirror the existing built-in pattern as closely as possible so the visual style stays consistent (UIStyleHelper.drawBeveledPanel for the indicator, context.drawTextWithShadow for labels, UITheme.BACKGROUND_TERTIARY fill on hover, same indicator/label x-offsets used at lines 835-847 and 883-895).

    Choose the integration that fits the existing selected-category model: the built-in sidebar uses a tab strip over `NodeCategory.values()` (line 586) plus a selected-category content panel. Since addon categories are runtime `AddonNodeCategory` instances (not enum values), render addon categories as additional sections after the built-in content. Concretely:

    1. In the content render region (the block guarded by `selectedCategory != null` starting at line 721, OR a parallel always-rendered addon block — pick whichever keeps built-in behavior unchanged), after the built-in category entries are drawn, iterate `addonCategoryNodes.entrySet()`. For each `AddonNodeCategory` key: draw a category header row (use getIcon() + getDisplayName(), colored with getColor()), then for each `AddonNodeDefinition` in the value list draw an entry row using the same row layout as built-in node rows (indicator via drawBeveledPanel using def.getColor(), label via drawTextWithShadow using def.getDisplayName()).

    2. For each addon entry row, compute `nodeHovered` with the same hit-test rectangle math used at lines 827-828 / 875-876 (effectiveMouseX/effectiveMouseY against nodeBackgroundLeft/nodeBackgroundRight and contentY..contentY+rowHeight). When hovered: set `hoveredAddonDefinition = def;` and fill the row background with UITheme.BACKGROUND_TERTIARY. Reset `hoveredAddonDefinition = null;` at the start of the addon render pass (mirroring how `hoveredNodeType = null;` is reset at line 758) so stale hovers clear.

    3. Include the addon entry rows in the scroll/clip math so they respect `sidebarBottom` and the scissor region (mirror the `if (contentY >= sidebarBottom) break;` guards).

    4. In `mouseClicked` (line 988), extend the drag-start guard from `if (hoveredNodeType != null || hoveredCustomNode != null)` to also include `|| hoveredAddonDefinition != null` so dragging an addon entry starts a drag and reaches `createNodeFromSidebar` (line 1028, which already returns `new Node(hoveredAddonDefinition.getId(), x, y)`).

    5. In the category-switch/collapse hover reset (line 980) and the mouse-leaves-sidebar reset (lines 951-955), also set `hoveredAddonDefinition = null;` so it does not stick across category changes or when the cursor leaves the sidebar.

    Do not change built-in category rendering behavior. Match Pathmind conventions: 4-space indent, Egyptian braces.
  </action>
  <verify>
    <automated>cd C:/Users/Trynda/Desktop/Dev/sidequests/pathmind && ./gradlew.bat :common:compileJava -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.java contains a loop over `addonCategoryNodes` (grep: `addonCategoryNodes.entrySet()` or `for (.*addonCategoryNodes` matches at least one render/hit-test site outside initializeAddonCategoryNodes and the test accessor)
    - Sidebar.java contains at least one non-null assignment `hoveredAddonDefinition = ` followed by a definition variable (not `null`) inside the hover hit-test — i.e. `grep -n "hoveredAddonDefinition = " ` shows a line whose RHS is not `null`
    - The mouseClicked drag-start guard at/near line 988 includes `hoveredAddonDefinition != null`
    - The mouse-leave reset block (near line 951) and the category-switch reset (near line 980) both clear hoveredAddonDefinition to null
    - `./gradlew.bat :common:compileJava` exits 0
  </acceptance_criteria>
  <done>An addon-declared category renders in the sidebar with its node entries; hovering an entry sets hoveredAddonDefinition non-null; clicking/dragging an entry starts a drag that creates the ADDON node via the existing createNodeFromSidebar branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| addon-provided render strings → DrawContext | AddonNodeCategory.getDisplayName()/getIcon() and AddonNodeDefinition.getDisplayName() supplied by third-party addon code, drawn per frame |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Denial of Service | Pathologically long addon displayName/icon causing layout overflow | accept | Rows are clipped to the scissor region and `sidebarBottom`; text overflow is cosmetic, not a crash. Built-in categories already trust their own label strings the same way (drawTextWithShadow does not crash on long input). |
| T-01-16 | Tampering | addon getColor() returning an arbitrary int | accept | Color is an ARGB int passed to drawBeveledPanel/drawTextWithShadow; no validation needed — an out-of-gamut value renders oddly but cannot escape the render call. |

No high-severity (ASVS L1 blocking) threats apply to this plan: it is render + hit-test wiring only, no input parsing, no persistence, no execution, no filesystem or network access. The data being rendered (addonCategoryNodes) was already validated at registration time by NodeTypeRegistrar (id format, null checks). Explicitly: no injection, deserialization, or privilege boundary is crossed by drawing the palette.
</threat_model>

<verification>
- `./gradlew.bat :common:compileJava` exits 0.
- `grep -n "hoveredAddonDefinition" common/src/main/java/com/pathmind/ui/sidebar/Sidebar.java` now shows at least one non-null assignment inside a hover branch (previously only the line 71 init and line 129 clear existed).
- `grep -n "addonCategoryNodes" common/src/main/java/com/pathmind/ui/sidebar/Sidebar.java` now shows a render/hit-test consumer beyond initializeAddonCategoryNodes and getAddonCategoryNodesForTest.
- Static read confirms the mouseClicked drag-start guard includes hoveredAddonDefinition.
</verification>

<success_criteria>
- CR-01 closed: addon categories are rendered, hoverable, and draggable from the sidebar.
- LUA-01 unblocked at the UI layer: the Script node can be placed from the palette (end-to-end execution requires Plan 01-04's snapshot fix; persistence requires Plan 01-04's load fix).
- API-07's renderer becomes reachable for nodes placed from the palette.
</success_criteria>

<output>
Create `.planning/phases/01-api-foundation-script-node-registration/01-05-SUMMARY.md` when done.
</output>
