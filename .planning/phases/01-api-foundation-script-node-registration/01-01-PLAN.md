---
phase: 01-api-foundation-script-node-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - common/src/main/java/com/pathmind/api/addon/PathmindAddonEntrypoint.java
  - common/src/main/java/com/pathmind/api/addon/NodeTypeRegistrar.java
  - common/src/main/java/com/pathmind/api/addon/AddonNodeDefinition.java
  - common/src/main/java/com/pathmind/api/addon/AddonNodeCategory.java
  - common/src/main/java/com/pathmind/api/addon/AddonNodeExecutor.java
  - common/src/main/java/com/pathmind/api/addon/AddonNodeSerializer.java
  - common/src/main/java/com/pathmind/api/addon/AddonNodeContext.java
  - common/src/main/java/com/pathmind/api/addon/AddonNodeBodyRenderer.java
  - common/src/main/java/com/pathmind/api/addon/NodeResult.java
  - common/src/main/java/com/pathmind/api/PathmindApiVersion.java
  - common/src/main/java/com/pathmind/nodes/NodeTypeRegistry.java
  - common/src/main/java/com/pathmind/execution/AddonLoader.java
  - fabric/src/main/java/com/pathmind/PathmindMod.java
  - common/src/test/java/com/pathmind/nodes/NodeTypeRegistryTest.java
autonomous: true
requirements: [API-01, API-02, API-03, API-04, API-08]
must_haves:
  truths:
    - "An addon mod declaring a \"pathmind\" entrypoint has its registerNodes(registrar) called at Pathmind mod init"
    - "A malformed registration (null arg, duplicate id, bad id format, thrown exception) disables only that addon and names it in the log; Pathmind keeps running"
    - "An addon declaring a pathmind dependency version below PathmindApiVersion.MIN_COMPATIBLE is disabled at the runtime check (D-11) before registerNodes runs, named in the log, and surfaced via the D-08 failure path; Pathmind keeps running"
    - "The registrar is sealed after init so no addon can register late"
    - "Pathmind init completes normally when zero addons are installed (empty entrypoint list)"
  artifacts:
    - path: "common/src/main/java/com/pathmind/api/addon/PathmindAddonEntrypoint.java"
      provides: "The single-method contract addon mods implement"
      contains: "void registerNodes(NodeTypeRegistrar"
    - path: "common/src/main/java/com/pathmind/api/addon/NodeTypeRegistrar.java"
      provides: "Mutable collector with validation + seal guard"
      contains: "register(AddonNodeDefinition"
    - path: "common/src/main/java/com/pathmind/nodes/NodeTypeRegistry.java"
      provides: "Singleton registry keyed by addon string id"
      contains: "public static final NodeTypeRegistry INSTANCE"
    - path: "common/src/main/java/com/pathmind/execution/AddonLoader.java"
      provides: "FabricLoader entrypoint discovery + per-addon failure isolation + D-11 runtime API-version check"
      contains: "getEntrypointContainers(\"pathmind\""
    - path: "common/src/main/java/com/pathmind/api/PathmindApiVersion.java"
      provides: "Independent API semver constants consumed by the D-11 runtime compatibility check"
      contains: "MIN_COMPATIBLE"
    - path: "common/src/test/java/com/pathmind/nodes/NodeTypeRegistryTest.java"
      provides: "Round-trip + failure-mode unit tests for the registrar/registry"
      contains: "class NodeTypeRegistryTest"
  key_links:
    - from: "fabric/src/main/java/com/pathmind/PathmindMod.java"
      to: "com.pathmind.execution.AddonLoader.discoverAndLoad()"
      via: "call appended at end of onInitialize"
      pattern: "AddonLoader.discoverAndLoad"
    - from: "common/src/main/java/com/pathmind/execution/AddonLoader.java"
      to: "com.pathmind.api.PathmindApiVersion.MIN_COMPATIBLE"
      via: "D-11 runtime API-version check before registerNodes"
      pattern: "PathmindApiVersion.MIN_COMPATIBLE"
    - from: "common/src/main/java/com/pathmind/execution/AddonLoader.java"
      to: "com.pathmind.nodes.NodeTypeRegistry.INSTANCE.install"
      via: "registrar sealed then installed"
      pattern: "NodeTypeRegistry.INSTANCE.install"
---

<objective>
Establish the addon API contract and discovery foundation: the `com.pathmind.api.addon` package (the types third-party addons compile against), the `NodeTypeRegistry` singleton that holds addon registrations, and the `AddonLoader` that discovers `"pathmind"` Fabric entrypoints at mod init with per-addon failure isolation and the D-11 runtime API-version check. This is the thinnest end-to-end backbone — after this plan, an addon mod can register a node type with Pathmind and Pathmind stays fully functional whether or not any addon is present.

Purpose: Without the API package and the loader, nothing else in the phase can exist — Plan 02 (node persistence/execution/sidebar) and Plan 03 (the Lua addon + Maven publish) both compile against these types. This plan is the foundation slice of the vertical that ends in "drop two jars, get a Script node."

Output: 10 new API/registry/loader files, the `AddonLoader` call wired into `PathmindMod.onInitialize`, and a JUnit test proving registration round-trips and every failure mode is handled.
</objective>

<execution_context>
@C:/Users/Trynda/Desktop/Dev/sidequests/pathmind/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Trynda/Desktop/Dev/sidequests/pathmind/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-api-foundation-script-node-registration/01-CONTEXT.md
@.planning/phases/01-api-foundation-script-node-registration/01-RESEARCH.md
@.planning/phases/01-api-foundation-script-node-registration/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

## Phase Goal

**As an** addon developer, **I want to** declare a `"pathmind"` entrypoint and register a node type through a typed registrar at Pathmind's mod init, **so that** my addon can extend Pathmind without Pathmind knowing my code at compile time — and a mistake in my addon never crashes Pathmind.

## Artifacts this phase produces

New symbols created in this plan (exclude from drift/source-grounding checks — they do not exist yet):

- Package `com.pathmind.api.addon` (new)
- `PathmindAddonEntrypoint` interface — method `void registerNodes(NodeTypeRegistrar)`
- `NodeTypeRegistrar` class — methods `register(AddonNodeDefinition, AddonNodeExecutor, AddonNodeSerializer)`, package-private `seal()`, package-private `getDefinitions()`/`getExecutors()`/`getSerializers()`
- `AddonNodeDefinition` class + nested `Builder` — fields `id`, `displayName`, `category`, `color`, `provenanceLabel`, `bodyRenderer`; method `builder(String id)`
- `AddonNodeCategory` class — fields `id`, `displayName`, `color`, `icon`
- `AddonNodeExecutor` functional interface — method `CompletableFuture<NodeResult> execute(AddonNodeContext)`
- `AddonNodeSerializer` interface — methods `Map<String,Object> serialize(AddonNodeContext)`, `void deserialize(AddonNodeContext, Map<String,Object>)`
- `AddonNodeContext` class — fields `addonTypeId`, `scriptText`
- `AddonNodeBodyRenderer` functional interface — method `void render(AddonNodeContext, DrawContext, int, int, int, int)`
- `NodeResult` enum — constants `SUCCESS`, `FAILURE`, `SKIPPED`
- `com.pathmind.api.PathmindApiVersion` class — constants `VERSION`, `MIN_COMPATIBLE`
- `com.pathmind.nodes.NodeTypeRegistry` class — `public static final NodeTypeRegistry INSTANCE`; methods `install(NodeTypeRegistrar)`, `hasType(String)`, `definitionFor(String)`, `executorFor(String)`, `serializerFor(String)`, `allDefinitions()`
- `com.pathmind.execution.AddonLoader` class — methods `discoverAndLoad()`, `markFailed(String, Throwable)`, `getFailure(String)`, `getFailedAddons()`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the com.pathmind.api.addon package (contract types + version)</name>
  <files>
    common/src/main/java/com/pathmind/api/addon/PathmindAddonEntrypoint.java,
    common/src/main/java/com/pathmind/api/addon/NodeTypeRegistrar.java,
    common/src/main/java/com/pathmind/api/addon/AddonNodeDefinition.java,
    common/src/main/java/com/pathmind/api/addon/AddonNodeCategory.java,
    common/src/main/java/com/pathmind/api/addon/AddonNodeExecutor.java,
    common/src/main/java/com/pathmind/api/addon/AddonNodeSerializer.java,
    common/src/main/java/com/pathmind/api/addon/AddonNodeContext.java,
    common/src/main/java/com/pathmind/api/addon/AddonNodeBodyRenderer.java,
    common/src/main/java/com/pathmind/api/addon/NodeResult.java,
    common/src/main/java/com/pathmind/api/PathmindApiVersion.java
  </files>
  <read_first>
    - .planning/phases/01-api-foundation-script-node-registration/01-PATTERNS.md (each file's exact analog + copy-ready code skeleton)
    - common/src/main/java/com/pathmind/nodes/NodeCategory.java (POJO field/accessor style to mirror for AddonNodeCategory)
    - common/src/main/java/com/pathmind/data/NodeGraphData.java (lines 112-200 — NodeData getter/setter POJO style)
    - .planning/codebase/CONVENTIONS.md (Pathmind code style — this code is upstream-bound, match it closely per D-03)
  </read_first>
  <behavior>
    - NodeTypeRegistrar.register rejects null def/exec/ser with NullPointerException ("All three registration arguments must be non-null")
    - NodeTypeRegistrar.register rejects an id not matching ^[a-z0-9_-]+:[a-z0-9_/.-]+$ with IllegalArgumentException
    - NodeTypeRegistrar.register rejects a duplicate id with IllegalArgumentException
    - NodeTypeRegistrar.register after seal() throws IllegalStateException
    - AddonNodeDefinition.Builder.build() throws if id, displayName, or category is missing
    - AddonNodeSerializer javadoc instructs callers to read numeric values via ((Number) fields.get(k)).intValue() (GSON Double erasure, Pitfall 4)
    - PathmindApiVersion exposes parseable semver constants VERSION and MIN_COMPATIBLE so AddonLoader's D-11 check can compare an addon-declared range against them
  </behavior>
  <action>
    Create the `com.pathmind.api.addon` package and the `PathmindApiVersion` class under `com.pathmind.api`. Implement exactly the types in 01-PATTERNS.md "Pattern Assignments":
    - `PathmindAddonEntrypoint`: `@FunctionalInterface` with `void registerNodes(NodeTypeRegistrar registrar)`. NO Minecraft imports.
    - `NodeTypeRegistrar`: `final` class holding three `LinkedHashMap<String,...>` (definitions/executors/serializers) plus a `boolean sealed` guard. Public `register(AddonNodeDefinition def, AddonNodeExecutor exec, AddonNodeSerializer ser)` enforces, in order: not-sealed (else IllegalStateException), all-non-null (else NullPointerException), id matches regex `^[a-z0-9_-]+:[a-z0-9_/.-]+$` (else IllegalArgumentException naming the bad id — this is the V5 path-traversal mitigation), id not already present (else IllegalArgumentException). Package-private `seal()`, package-private `getDefinitions()`/`getExecutors()`/`getSerializers()` returning `Collections.unmodifiableMap`.
    - `AddonNodeDefinition`: immutable POJO with nested static `Builder` (fields id, displayName, category, color default 0xFF888888, provenanceLabel default "", bodyRenderer default null). `builder(String id)` factory. `build()` validates id/displayName/category non-blank/non-null.
    - `AddonNodeCategory`: `final` POJO with fields id/displayName/color/icon + getters (no enum — runtime instances, per Pitfall 3 and D-05).
    - `AddonNodeExecutor`: `@FunctionalInterface` `CompletableFuture<NodeResult> execute(AddonNodeContext ctx)`. Javadoc: must never block the game thread.
    - `AddonNodeSerializer`: interface with `Map<String,Object> serialize(AddonNodeContext)` and `void deserialize(AddonNodeContext, Map<String,Object>)`. Javadoc MUST state the serialize map must include a `"_schema_version"` integer key, and deserialize must read numbers via `((Number) fields.get(k)).intValue()` (Pitfall 4).
    - `AddonNodeContext`: `final` POJO with fields `addonTypeId` and `scriptText` + getters/setters. This is the ONLY API class that an executor reaches Pathmind state through — it must NOT expose Node, ExecutionManager, or NodeGraph (anti-pattern from RESEARCH.md).
    - `AddonNodeBodyRenderer`: `@FunctionalInterface` `void render(AddonNodeContext ctx, DrawContext draw, int x, int y, int width, int height)`. This is the ONE API type that imports `net.minecraft.client.gui.DrawContext` (rendering hook, API-07).
    - `NodeResult`: enum SUCCESS, FAILURE, SKIPPED with javadoc per constant.
    - `PathmindApiVersion`: `final` class, private ctor, `public static final String VERSION = "0.1.0"` and `MIN_COMPATIBLE = "0.1.0"` (D-10 independent semver). These are the constants AddonLoader's D-11 runtime check (Task 2) compares an addon's declared `pathmind` dependency range against — keep them as plain dotted-semver strings so a SemanticVersion parse is straightforward.
    Add javadoc to every public type and method (API-10 documentation requirement starts here). Reference D-02 (registrar pattern), D-03 (upstream code style), D-10 (independent semver) in commit message.
  </action>
  <verify>
    <automated>cd C:/Users/Trynda/Desktop/Dev/sidequests/pathmind && ./gradlew :common:compileJava -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 10 files exist under common/src/main/java/com/pathmind/api/
    - PathmindAddonEntrypoint.java contains `void registerNodes(NodeTypeRegistrar`
    - NodeTypeRegistrar.java contains the regex literal `^[a-z0-9_-]+:[a-z0-9_/.-]+$`
    - AddonNodeContext.java does NOT contain any import of `com.pathmind.nodes.Node`, `com.pathmind.execution.ExecutionManager`, or `com.pathmind.ui.graph.NodeGraph` (grep returns no match)
    - AddonNodeBodyRenderer.java contains `import net.minecraft.client.gui.DrawContext`
    - PathmindApiVersion.java contains `VERSION = "0.1.0"` and `MIN_COMPATIBLE`
    - `./gradlew :common:compileJava` exits 0
  </acceptance_criteria>
  <done>The com.pathmind.api package compiles. Every addon-facing type exists with javadoc, validation guards in the registrar, and zero impl-class leakage in AddonNodeContext.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NodeTypeRegistry singleton + AddonLoader discovery (incl. D-11 runtime API-version check), wired into PathmindMod</name>
  <files>
    common/src/main/java/com/pathmind/nodes/NodeTypeRegistry.java,
    common/src/main/java/com/pathmind/execution/AddonLoader.java,
    fabric/src/main/java/com/pathmind/PathmindMod.java,
    common/src/test/java/com/pathmind/nodes/NodeTypeRegistryTest.java
  </files>
  <read_first>
    - common/src/main/java/com/pathmind/nodes/NodeBehaviorDefinitionRegistry.java (exact analog for NodeTypeRegistry singleton/map pattern)
    - fabric/src/main/java/com/pathmind/PathmindMod.java (current onInitialize — AddonLoader call goes at line 29, before the final LOGGER.info)
    - common/src/test/java/com/pathmind/nodes/NodeBehaviorDefinitionRegistryTest.java (test class structure + method-naming style to mirror)
    - common/src/main/java/com/pathmind/api/PathmindApiVersion.java (from Task 1 — VERSION/MIN_COMPATIBLE constants the D-11 check reads)
    - .planning/phases/01-api-foundation-script-node-registration/01-RESEARCH.md ("Open Questions (RESOLVED)" + the D-11 runtime check row in Security Domain + the architecture diagram showing the per-container compatibility check)
    - .planning/phases/01-api-foundation-script-node-registration/01-PATTERNS.md (AddonLoader copy-ready skeleton + the note that the overlay surfacing happens later, from the editor-open path, not from AddonLoader)
  </read_first>
  <behavior>
    - NodeTypeRegistry.INSTANCE.install(registrar) copies definitions/executors/serializers; a second install() throws IllegalStateException
    - hasType returns true only for installed ids; definitionFor/executorFor/serializerFor return the registered instances
    - A registrar that successfully registered "test_mod:script" installs and the registry returns the executor and serializer by that id
    - Duplicate-id registration throws IllegalArgumentException (registrar-level)
    - Null executor registration throws NullPointerException (registrar-level)
    - Registration after seal() throws IllegalStateException
    - Malformed id "../../../evil:id" is rejected by the registrar (IllegalArgumentException)
    - D-11: when an addon container declares a pathmind dependency whose accepted range excludes PathmindApiVersion.MIN_COMPATIBLE (i.e., the addon targets an API below the minimum compatible), AddonLoader skips registerNodes for that addon, records it via markFailed with an informative reason, and continues with other addons — Pathmind init still completes
  </behavior>
  <action>
    Create `NodeTypeRegistry` in `com.pathmind.nodes` exactly per 01-PATTERNS.md: public singleton `INSTANCE`, private ctor, three `LinkedHashMap<String,...>`, a `boolean installed` guard. `install(NodeTypeRegistrar registrar)` throws IllegalStateException if already installed, else `putAll` from the registrar's package-private getters and sets installed=true. Public accessors `hasType`, `definitionFor`, `executorFor`, `serializerFor`, `allDefinitions()` (unmodifiable). Because `install` reads the registrar's package-private getters, `NodeTypeRegistry` must be in `com.pathmind.nodes` while the registrar getters are package-private in `com.pathmind.api.addon` — expose them by making the registrar's `getDefinitions/getExecutors/getSerializers` public OR add a package-private bridge; choose making them public on the registrar (they return unmodifiable maps, so this is safe) and document that addons must not call them.

    Create `AddonLoader` in `com.pathmind.execution` per the 01-PATTERNS.md skeleton: static `discoverAndLoad()` builds a `NodeTypeRegistrar`, calls `FabricLoader.getInstance().getEntrypointContainers("pathmind", PathmindAddonEntrypoint.class)`, and iterates the containers. For each container, BEFORE invoking `registerNodes`, run the **D-11 runtime API-version check**:
    - Resolve the addon id via `container.getProvider().getMetadata().getId()`.
    - Read the addon's declared dependency on Pathmind's API from `container.getProvider().getMetadata().getDependencies()` — find the `ModDependency` whose `getModId()` equals `"pathmind"` (the D-04/D-11 dependency key the addon declares in fabric.mod.json; Plan 03 Task 2 sets it). Its `getVersionRequirements()` / accepted `VersionInterval`s express the addon's declared compatible API range.
    - Parse `PathmindApiVersion.MIN_COMPATIBLE` with Fabric's `net.fabricmc.loader.api.SemanticVersion.parse(...)` and test whether the addon's declared range admits it (e.g., the dependency's `matches(minCompatibleVersion)`; if the API does not offer a direct `matches`, evaluate the parsed `VersionInterval`s). If the addon declares NO `pathmind` dependency at all, treat that as a soft-pass for Phase 1 (log at debug and proceed) — the Fabric-loader half of D-11 only fires when a dependency IS declared, and the hard-mismatch blocking is the loader's job; this runtime layer specifically catches addons whose declared range is below MIN_COMPATIBLE.
    - If the check FAILS (declared range excludes MIN_COMPATIBLE): log `LOGGER.error("[Pathmind] Addon '{}' targets an incompatible Pathmind API version (requires {}, this build provides {}) — addon disabled", addonId, declaredRange, PathmindApiVersion.VERSION)`, call `markFailed(addonId, new IllegalStateException("Incompatible Pathmind API version: ..."))`, and `continue` to the next container WITHOUT calling registerNodes (this routes the addon through the same D-08 failure UX that Plan 02 surfaces from `getFailedAddons()`).
    - Wrap any exception thrown by the dependency-introspection itself in the same per-addon `catch (Throwable)` so a malformed metadata read disables only that addon.

    For containers that PASS the D-11 check, call `container.getEntrypoint().registerNodes(registrar)` inside a `try { ... } catch (Throwable t)` that logs `LOGGER.error("[Pathmind] Addon '{}' failed node registration — addon disabled", addonId, t)` and calls `markFailed(addonId, t)` (API-03 names the offending addon). After the loop, `registrar.seal()` then `NodeTypeRegistry.INSTANCE.install(registrar)` (API-04 — sealing after init guarantees no late registration; calling this at the END of onInitialize sidesteps Fabric entrypoint ordering per Pattern 5). Keep a static `LinkedHashMap<String,Throwable> failedAddons` with `markFailed`, `getFailure`, `getFailedAddons` (unmodifiable). Do NOT call the NodeErrorNotificationOverlay here — AddonLoader runs before the client is ready; the editor-open surfacing is Plan 02's concern. The empty-entrypoint-list case (no addon installed) naturally produces an empty registrar and a clean install — this is the API-09 standalone path.

    Modify `PathmindMod.onInitialize`: insert `com.pathmind.execution.AddonLoader.discoverAndLoad();` after the version-support check and BEFORE the final `LOGGER.info("Pathmind mod initialized successfully")` (current line 29). Do not reorder any existing line. Reference API-01/API-03/API-04/D-11 in the commit message.

    Write `NodeTypeRegistryTest` in `common/src/test/java/com/pathmind/nodes/`: cover registeredAddonTypeIsRetrievableByStringId, duplicateAddonTypeIdThrowsIllegalArgumentException, nullExecutorThrowsNullPointerException, registrationAfterSealThrowsIllegalStateException, malformedAddonTypeIdIsRejected (uses "../../../evil:id"), and a doubleInstallThrows test. Construct the registrar directly in tests (it is in the same module). Build a minimal valid AddonNodeDefinition via the builder, a no-op AddonNodeExecutor lambda returning CompletableFuture.completedFuture(NodeResult.SUCCESS), and a trivial AddonNodeSerializer. To exercise seal()/install() (package-private), place the test in a package that can reach them or add a package-private test helper — NodeTypeRegistry.install is public so install can be tested; for seal(), register-after-seal can be tested by calling install (which is not what seals the registrar) — instead test seal by reflection OR add a package-private `sealForTest()`; prefer testing the registrar's sealed behavior by making the test live in com.pathmind.api.addon package so it can call seal() directly. Split the test across the registry test (public API) and, if needed, a second registrar test in com.pathmind.api.addon — but keep the named file NodeTypeRegistryTest.java covering at least the four registrar validation behaviors via a registrar instance plus the double-install guard. The D-11 version-comparison logic is best covered by a small pure helper (e.g., a package-private `static boolean isApiCompatible(addonDeclaredRange)` on AddonLoader) unit-tested directly with parsed SemanticVersion inputs, since the full FabricLoader container path is not available under `:common:test`; if extracting such a helper, add a focused test asserting a below-MIN_COMPATIBLE range fails and an at-or-above range passes.
  </action>
  <verify>
    <automated>cd C:/Users/Trynda/Desktop/Dev/sidequests/pathmind && ./gradlew :common:test --tests "com.pathmind.nodes.NodeTypeRegistryTest" -q 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - NodeTypeRegistry.java contains `public static final NodeTypeRegistry INSTANCE`
    - AddonLoader.java contains `getEntrypointContainers("pathmind"` and `NodeTypeRegistry.INSTANCE.install`
    - AddonLoader.java contains `PathmindApiVersion.MIN_COMPATIBLE` and a pre-registration D-11 check that calls `markFailed` and `continue`s for an incompatible addon (grep finds both `MIN_COMPATIBLE` and `getDependencies` / the dependency-introspection call)
    - AddonLoader.java contains a `catch (Throwable` block that logs the addon id and calls `markFailed`
    - PathmindMod.java contains `AddonLoader.discoverAndLoad()` and it appears before the `Pathmind mod initialized successfully` log line
    - `./gradlew :common:test --tests "com.pathmind.nodes.NodeTypeRegistryTest"` exits 0 with all named test methods passing (incl. the D-11 helper test if a compatibility helper was extracted)
    - `./gradlew :common:test` (full suite) exits 0 — no regression in the existing 24 test files
  </acceptance_criteria>
  <done>An addon entrypoint is discovered and invoked at mod init; an addon declaring an API range below MIN_COMPATIBLE is disabled at the D-11 runtime check before registration and recorded for the D-08 failure UX; other failures are isolated per-addon and logged with the addon id; the registry seals after init; the unit test proves every registration round-trip and failure mode. Pathmind init is unchanged when no addon is present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party addon jar → Pathmind registry | Untrusted addon code runs `registerNodes(registrar)` inside Pathmind's mod-init thread; supplies node ids, definitions, executors, serializers |
| Fabric loader → AddonLoader | Entrypoint instances are constructed and invoked by Pathmind; a malicious/buggy addon may throw, return null, supply hostile ids, or declare an incompatible API version range |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | NodeTypeRegistrar.register (addon-supplied node id) | mitigate | Validate id against `^[a-z0-9_-]+:[a-z0-9_/.-]+$` before insertion — blocks path-traversal / shadowing ids (ASVS V5). Reject with IllegalArgumentException naming the bad id |
| T-01-02 | Tampering | NodeTypeRegistrar.register (duplicate id shadowing a built-in or another addon) | mitigate | Duplicate-id check throws IllegalArgumentException; whole addon disabled via per-addon try/catch (D-08) |
| T-01-03 | Tampering | NodeTypeRegistrar.register (null executor/serializer → silent execution skip) | mitigate | Null check throws NullPointerException; caught and isolated per addon |
| T-01-04 | Denial of Service | AddonLoader.discoverAndLoad (addon throws during registration) | mitigate | Per-container `catch (Throwable)` — one bad addon cannot abort Pathmind init or other addons; logged with addon id (API-03) |
| T-01-05 | Elevation of Privilege | NodeTypeRegistrar usable after init (late registration) | mitigate | `seal()` after all entrypoints run; post-seal register() throws IllegalStateException (ASVS V4) |
| T-01-13 | Spoofing | Addon declares an incompatible Pathmind API version range (D-11 runtime layer) | mitigate | Before calling registerNodes, AddonLoader reads the container's declared `pathmind` dependency range and compares against `PathmindApiVersion.MIN_COMPATIBLE`; an addon below the minimum is disabled via markFailed + the D-08 failure UX, never registering nodes. Complements the Fabric-loader hard-mismatch block (declared in Plan 03 fabric.mod.json) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — Pathmind adds zero new external dependencies (RESEARCH.md Package Legitimacy Audit). No slopcheck needed |
</threat_model>

<verification>
- `./gradlew :common:compileJava` and `./gradlew :common:test` both exit 0
- `NodeTypeRegistryTest` passes all registration round-trip and failure-mode cases (and the D-11 compatibility-helper case if extracted)
- Grep confirms AddonNodeContext leaks no impl classes; AddonLoader names the failing addon id in its catch block and references `PathmindApiVersion.MIN_COMPATIBLE` in the D-11 check
- `PathmindMod.onInitialize` calls AddonLoader.discoverAndLoad() as the last step before the success log
</verification>

<success_criteria>
- The `com.pathmind.api.addon` package exists and compiles with full javadoc
- An addon's `registerNodes` is invoked at Pathmind init via the `"pathmind"` entrypoint
- Malformed / duplicate / null / late / throwing / API-incompatible registrations are each rejected and isolated, with the offending addon named in the log
- The D-11 runtime API-version check disables an addon declaring a range below MIN_COMPATIBLE before it registers, routing it through the D-08 failure path
- Pathmind init completes cleanly with zero addons installed
- Full existing test suite stays green
</success_criteria>

<output>
Create `.planning/phases/01-api-foundation-script-node-registration/01-01-SUMMARY.md` when done
</output>
</content>
</invoke>
