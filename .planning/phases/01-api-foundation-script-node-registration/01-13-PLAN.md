---
phase: 01-api-foundation-script-node-registration
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - common/src/test/java/com/pathmind/data/AddonNodePersistenceTest.java
autonomous: true
requirements: [API-05, LUA-05]
gap_closure: true

must_haves:
  truths:
    - "The JSON-field presence checks proving addonTypeId, _schema_version, and extraFields appear in serialized preset JSON execute unconditionally (without -ea) and fail the build if the JSON is wrong (NEW-WR-01 / WR-01)"
  artifacts:
    - path: "common/src/test/java/com/pathmind/data/AddonNodePersistenceTest.java"
      provides: "JSON-field presence assertions converted from Java assert to executable JUnit assertions"
      contains: "assertTrue"
  key_links:
    - from: "common/src/test/java/com/pathmind/data/AddonNodePersistenceTest.java"
      to: "org.junit.jupiter.api.Assertions"
      via: "static assertTrue/assertFalse imports replacing Java assert"
      pattern: "import static org\\.junit\\.jupiter\\.api\\.Assertions"
---

<objective>
Close NEW-WR-01 / WR-01 (regression — test integrity): `AddonNodePersistenceTest` uses the Java `assert` keyword for critical JSON-field presence checks at lines 179-182 (four asserts: addonTypeId present, addon id present, _schema_version present, extraFields present) and lines 221-222 (two asserts: built-in node JSON must NOT contain addonTypeId / extraFields). The `assert` keyword is disabled by default (`-ea` not set under Gradle test), so these six checks silently pass even if the serialized JSON is missing the addon fields entirely. They are the only automated proof that addonTypeId, _schema_version, and extraFields appear in serialized preset JSON (the API-05 / LUA-05 persistence contract).

Purpose: Make these six checks executable so the build fails when the persistence JSON contract regresses.
Output: The six Java `assert` statements replaced with equivalent JUnit assertions (`assertTrue` / `assertFalse`) using the suite's existing static-import style.
</objective>

<execution_context>
@C:/Users/Trynda/Desktop/Dev/sidequests/pathmind/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Trynda/Desktop/Dev/sidequests/pathmind/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-api-foundation-script-node-registration/01-VERIFICATION.md
@.planning/phases/01-api-foundation-script-node-registration/01-REVIEW.md

@common/src/test/java/com/pathmind/data/AddonNodePersistenceTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Java assert with JUnit assertions in AddonNodePersistenceTest</name>
  <files>common/src/test/java/com/pathmind/data/AddonNodePersistenceTest.java</files>
  <read_first>
    - common/src/test/java/com/pathmind/data/AddonNodePersistenceTest.java — the method `nodeData_addonTypeIdAndExtraFieldsSurviveGsonRoundTrip` (asserts at lines 179-182: `assert json.contains("addonTypeId")`, `assert json.contains(TEST_ADDON_ID)`, `assert json.contains("_schema_version")`, `assert json.contains("extraFields")`) and `nodeData_builtinNodeHasNoAddonFields` (asserts at lines 221-222: `assert !json.contains("addonTypeId")`, `assert !json.contains("extraFields")`). Note the existing static imports (assertEquals, assertNotNull, assertNull at lines 21-23) — `assertTrue` and `assertFalse` are NOT yet imported.
    - The org.junit.jupiter.api.Assertions static-import style already used elsewhere in this suite and in AddonNodeConversionRoundTripTest (which imports assertTrue and assertFalse the same way).
  </read_first>
  <action>
    Add the two missing static imports `import static org.junit.jupiter.api.Assertions.assertTrue;` and `import static org.junit.jupiter.api.Assertions.assertFalse;` alongside the existing Assertions imports (keep alphabetical/grouped ordering consistent with the file).

    In `nodeData_addonTypeIdAndExtraFieldsSurviveGsonRoundTrip`, replace the four `assert json.contains(...) : "..."` statements with `assertTrue(json.contains(...), "...")` preserving each existing failure message verbatim (JSON must contain addonTypeId / the addon type id / _schema_version / extraFields).

    In `nodeData_builtinNodeHasNoAddonFields`, replace the two `assert !json.contains(...) : "..."` statements with `assertFalse(json.contains(...), "...")` preserving each existing message verbatim (Built-in node JSON must not contain addonTypeId / extraFields).

    Do not change any test logic, the GSON setup, or the existing assertEquals/assertNotNull/assertNull calls. Note: the unrelated `assert version >= 1;` inside the inline TEST_SERIALIZER.deserialize (line ~56) is a serializer-internal sanity check, not a JSON-field presence test under remediation — leave it unless trivially converting it to assertTrue is consistent; the in-scope WR-01 fix is the six JSON-field presence/absence checks. Follow project conventions (4-space indent).
  </action>
  <verify>
    <automated>cd C:/Users/Trynda/Desktop/Dev/sidequests/pathmind && ./gradlew.bat :common:test --tests "com.pathmind.data.AddonNodePersistenceTest"</automated>
  </verify>
  <acceptance_criteria>
    - AddonNodePersistenceTest.java contains `import static org.junit.jupiter.api.Assertions.assertTrue;` and `import static org.junit.jupiter.api.Assertions.assertFalse;`.
    - `nodeData_addonTypeIdAndExtraFieldsSurviveGsonRoundTrip` contains four `assertTrue(json.contains(...), ...)` calls and no `assert json.contains` statements.
    - `nodeData_builtinNodeHasNoAddonFields` contains two `assertFalse(json.contains(...), ...)` calls and no `assert !json.contains` statements.
    - No occurrence of the Java `assert` keyword applied to `json.contains(...)` remains anywhere in the file.
    - `./gradlew.bat :common:test --tests "com.pathmind.data.AddonNodePersistenceTest"` exits 0 (all 6 tests pass; the now-executable JSON checks still hold against the correct serializer).
  </acceptance_criteria>
  <done>The six JSON-field presence/absence checks are executable JUnit assertions that run without -ea and would fail the build if the persistence JSON contract regressed; the suite passes.</done>
</task>

</tasks>

<artifacts_produced>
No new symbols. Six Java `assert` statements within existing test methods `nodeData_addonTypeIdAndExtraFieldsSurviveGsonRoundTrip` and `nodeData_builtinNodeHasNoAddonFields` are converted to JUnit `assertTrue`/`assertFalse`. Two new static imports (`assertTrue`, `assertFalse`) added. No new test methods or classes.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → CI build gate | The serialized-JSON presence checks are the automated proof of the API-05/LUA-05 persistence contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13-01 | Repudiation / Tampering | silent-pass test assertions | mitigate | Converting Java `assert` to JUnit assertions makes the JSON-contract checks executable under Gradle (no -ea), so a regression that drops addonTypeId/_schema_version/extraFields from preset JSON is caught at build time rather than passing silently |
| T-01-13-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan; no dependency changes |

No high-severity threats. Test-only change with no production code or runtime surface. There is no untrusted-input, network, auth, or secret surface here.
</threat_model>

<verification>
- `./gradlew.bat :common:test --tests "com.pathmind.data.AddonNodePersistenceTest"` exits 0.
- Source inspection: no `assert json.contains` / `assert !json.contains` statements remain; replaced by assertTrue/assertFalse with the original messages; assertTrue and assertFalse statically imported.
</verification>

<success_criteria>
- NEW-WR-01 / WR-01 closed: the six JSON-field presence/absence checks are executable JUnit assertions that fail the build on a persistence-contract regression.
- AddonNodePersistenceTest passes; no test logic changed beyond the assertion mechanism.
</success_criteria>

<output>
Create `.planning/phases/01-api-foundation-script-node-registration/01-13-SUMMARY.md` when done.
</output>
