---
phase: 03-script-node-editor-autosuggestions
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/test/java/com/mrmysterium/pathmindlua/SuggestionEngineTest.java
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/test/java/com/mrmysterium/pathmindlua/GutterWidthTest.java
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/test/java/com/mrmysterium/pathmindlua/LuaNodeSerializerLastErrorTest.java
autonomous: true
requirements: [EDIT-02, EDIT-03, EDIT-04]
repos: [pathmind-lua]

must_haves:
  truths:
    - "The addon test suite contains executable JUnit tests that assert SuggestionEngine prefix-match behavior, gutter-width formula, and lastError serialize/deserialize round-trip"
    - "Each scaffolded test fails (or is @Disabled with a TODO) until its production code lands in later waves — proving the test gates the behavior"
  artifacts:
    - path: "C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/test/java/com/mrmysterium/pathmindlua/SuggestionEngineTest.java"
      provides: "EDIT-04 prefix-match completion assertions (keywords, stdlib, pathmind.* member access)"
    - path: "C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/test/java/com/mrmysterium/pathmindlua/GutterWidthTest.java"
      provides: "EDIT-02 gutter-width formula assertions for 1/2/3-digit line counts"
    - path: "C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/test/java/com/mrmysterium/pathmindlua/LuaNodeSerializerLastErrorTest.java"
      provides: "EDIT-03 lastError + lastErrorLine round-trip assertions (schema v2)"
  key_links:
    - from: "GutterWidthTest"
      to: "computeGutterWidth formula (digitCount * 6 + 8)"
      via: "static formula assertion mirrored in test until LuaScriptNodeRenderer exposes it"
      pattern: "assertEquals.*14|assertEquals.*20|assertEquals.*26"
---

<objective>
Wave 0 test scaffold for the addon-side logic that later waves implement: the in-process autosuggestion engine (EDIT-04), the gutter-width formula (EDIT-02), and the `lastError` persistence round-trip (EDIT-03). This satisfies the Nyquist rule — every later `<verify>` references a concrete automated test that exists from Wave 0.

These are pure-JVM unit tests (no Minecraft runtime needed), keeping them fast and CI-safe, exactly like the existing `pathmind-lua` VM tests (`PathmindBindingsVarTest`, `CobaltVmSmokeTest`).

Purpose: Lock the behavioral contracts for EDIT-02/03/04 as executable gates BEFORE the production code lands, so regressions fail the build.
Output: Three JUnit test files in the addon repo's test tree.

REPO: pathmind-lua (sibling addon repo, Fabric MC 1.21.4 only). All paths absolute.
</objective>

<execution_context>
@C:/Users/Trynda/Desktop/Dev/sidequests/pathmind/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Trynda/Desktop/Dev/sidequests/pathmind/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-script-node-editor-autosuggestions/03-RESEARCH.md
@.planning/phases/03-script-node-editor-autosuggestions/03-UI-SPEC.md
@C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/test/java/com/mrmysterium/pathmindlua/vm/PathmindBindingsVarTest.java
</context>

<artifacts_this_phase_produces>
This plan produces (NEW symbols, pathmind-lua repo):
- `com.mrmysterium.pathmindlua.SuggestionEngineTest` — JUnit test class
- `com.mrmysterium.pathmindlua.GutterWidthTest` — JUnit test class
- `com.mrmysterium.pathmindlua.LuaNodeSerializerLastErrorTest` — JUnit test class

These reference symbols created in later plans (`SuggestionEngine`, `LuaScriptNodeRenderer.computeGutterWidth`, `LuaNodeSerializer` v2 `lastError`/`lastErrorLine`). Until those land, tests are marked `@Disabled("Wave N — enable when <symbol> lands")` with the assertions written out, OR (preferred) the formula tests that depend only on pure math are written as live tests against a local helper copy of the formula and re-pointed at the production symbol in the owning wave.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold SuggestionEngine + GutterWidth tests (EDIT-04, EDIT-02)</name>
  <files>
  C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/test/java/com/mrmysterium/pathmindlua/SuggestionEngineTest.java
  C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/test/java/com/mrmysterium/pathmindlua/GutterWidthTest.java
  </files>
  <read_first>
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/test/java/com/mrmysterium/pathmindlua/vm/PathmindBindingsVarTest.java (JUnit Jupiter idiom in this repo: package, imports, @Test, assertEquals)
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind/.planning/phases/03-script-node-editor-autosuggestions/03-RESEARCH.md (Pattern 7: tokenizer rules + symbol set; Pattern 6: gutter-width formula `digitCount * 6 + 8`)
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind/.planning/phases/03-script-node-editor-autosuggestions/03-UI-SPEC.md (Spacing Scale: gutter 1-digit=14px, 2-digit=20px, 3-digit=26px; Copywriting Contract: full keyword + stdlib + pathmind.* symbol lists)
  </read_first>
  <behavior>
  SuggestionEngineTest:
    - Test: input ending with "pathmind." returns ALL six pathmind.* names (getBlock, getInventory, getPosition, getVar, moveTo, setVar) — member-access auto-trigger.
    - Test: input "pathmind.getB" returns only [getBlock] — member sub-prefix filter, case-insensitive.
    - Test: plain prefix "fun" returns [function] from the Lua keyword set; "pri" returns [print] from stdlib.
    - Test: prefix with no match returns an empty list (popup closes).
    - Test: plain-prefix results capped at 6 entries.
  GutterWidthTest:
    - Test: lineCount 1..9 → gutter width 14; 10..99 → 20; 100..999 → 26 (formula digitCount*6+8).
  </behavior>
  <action>
  Create both test classes in package `com.mrmysterium.pathmindlua`. Match the JUnit Jupiter import + `@Test` + `assertEquals`/`assertIterableEquals` style from PathmindBindingsVarTest. SuggestionEngineTest asserts against the `SuggestionEngine` API that Plan 05 (03-05) will create — instantiate `new SuggestionEngine()` and call its `getSuggestions(String lineBeforeCursor)` returning a `List<CompletionEntry>` where `CompletionEntry.name()` is the function/keyword name. Until SuggestionEngine exists, annotate the class `@org.junit.jupiter.api.Disabled("Wave 4 — enable when SuggestionEngine lands (03-05)")` AND keep all assertion bodies fully written so enabling is a one-line change. GutterWidthTest depends only on the pure formula `digitCount * 6 + 8` from RESEARCH Pattern 6 / UI-SPEC; write it as a LIVE (non-disabled) test against a private static helper `gutterWidth(int lineCount)` copied into the test, asserting 14/20/26 for representative counts — Plan 04 (03-04) will re-point this at `LuaScriptNodeRenderer.computeGutterWidth` once that method is public/package-visible. Do NOT hardcode the digit boundary wrong: 9→14, 10→20, 99→20, 100→26. No fenced code in production; test bodies are fine.
  </action>
  <verify>
    <automated>cd C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua && ./gradlew test --tests "com.mrmysterium.pathmindlua.GutterWidthTest"</automated>
  </verify>
  <acceptance_criteria>
    - `GutterWidthTest` compiles and PASSES (live test, pure math) asserting 14/20/26 across 1/2/3-digit boundaries.
    - `SuggestionEngineTest` compiles (class present, @Disabled with assertion bodies written) — `./gradlew compileTestJava` succeeds in pathmind-lua.
    - No reference to Minecraft client classes in either test (pure-JVM, runs without a game).
  </acceptance_criteria>
  <done>GutterWidthTest passes; SuggestionEngineTest compiles with full assertion bodies pending Wave 4 enablement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold lastError round-trip test (EDIT-03)</name>
  <files>
  C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/test/java/com/mrmysterium/pathmindlua/LuaNodeSerializerLastErrorTest.java
  </files>
  <read_first>
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua/src/main/java/com/mrmysterium/pathmindlua/LuaNodeSerializer.java (current schema v1 serialize/deserialize; the contract this test extends)
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind/common/src/main/java/com/pathmind/api/addon/AddonNodeContext.java (the context object the serializer reads/writes; lastError/lastErrorLine getters/setters added in Plan 02)
  - C:/Users/Trynda/Desktop/Dev/sidequests/pathmind/.planning/phases/03-script-node-editor-autosuggestions/03-PATTERNS.md (LuaNodeSerializer MODIFY section: exact lastError serialize/deserialize lines, schema bump to 2)
  </read_first>
  <behavior>
    - Test: serialize an AddonNodeContext with lastError="boom" and lastErrorLine=7 → the returned map contains "lastError"="boom", "lastErrorLine"=7, and "_schema_version"=2.
    - Test: deserialize a map with "lastError"="x"/"lastErrorLine"=3.0 (GSON Double) → ctx.getLastError()=="x", ctx.getLastErrorLine()==3 (Number.intValue cast survives GSON erasure).
    - Test: deserialize a v1 map (no lastError keys) → ctx.getLastError()==null, ctx.getLastErrorLine()==0 (backward compatible).
    - Test: round-trip success path — serialize ctx with null lastError → deserialize → getLastError()==null (cleared-on-success contract).
  </behavior>
  <action>
  Create `LuaNodeSerializerLastErrorTest` in package `com.mrmysterium.pathmindlua`. Construct `new LuaNodeSerializer()` and `new AddonNodeContext()` (the API class on the compileOnly classpath). Assert the serialize map keys and the deserialize round-trip including the GSON `Double`→`int` boundary (pass `3.0d` into the map to mimic GSON). Assert backward-compat with a v1 map missing both lastError keys. The test references `ctx.getLastError()`, `ctx.setLastError(String)`, `ctx.getLastErrorLine()`, `ctx.setLastErrorLine(int)` (added to AddonNodeContext in Plan 02) and serializer schema v2 (Plan 04). Annotate `@org.junit.jupiter.api.Disabled("Wave 3 — enable when AddonNodeContext lastError fields + LuaNodeSerializer v2 land (03-02, 03-04)")` with all assertion bodies fully written. Use `assertEquals` and `((Number) map.get("lastErrorLine")).intValue()` exactly as the codebase mandates (never a direct Integer cast).
  </action>
  <verify>
    <automated>cd C:/Users/Trynda/Desktop/Dev/sidequests/pathmind-lua && ./gradlew compileTestJava</automated>
  </verify>
  <acceptance_criteria>
    - `LuaNodeSerializerLastErrorTest` compiles (class present, @Disabled, assertion bodies written referencing schema v2 + lastError context fields).
    - Test uses `((Number) ...).intValue()` for the line-number read (no direct Integer cast).
    - `./gradlew compileTestJava` succeeds in pathmind-lua with all three new test files present.
  </acceptance_criteria>
  <done>All three Wave 0 test files compile; GutterWidthTest passes live; the other two are written-and-disabled, ready for one-line enablement in their owning waves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → no game/network | Pure-JVM unit tests; no untrusted input, no I/O, no Minecraft runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | Test asserting wrong gutter boundary masks a real EDIT-02 bug | mitigate | Boundary cases pinned explicitly: 9→14, 10→20, 99→20, 100→26 (off-by-one digit boundaries are the failure mode) |
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; no new dependencies (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `cd pathmind-lua && ./gradlew compileTestJava` succeeds with all three new test files.
- `cd pathmind-lua && ./gradlew test --tests "com.mrmysterium.pathmindlua.GutterWidthTest"` passes.
- Disabled tests carry a `Wave N — enable when <symbol> lands (03-0X)` reason string so the owning plan can find them.
</verification>

<success_criteria>
- Executable JUnit gates exist for EDIT-02 (gutter), EDIT-03 (lastError persistence), EDIT-04 (suggestions) before any production code lands.
- GutterWidth math is verified live; the two symbol-dependent suites compile and are ready to enable.
</success_criteria>

<output>
Create `.planning/phases/03-script-node-editor-autosuggestions/03-01-SUMMARY.md` when done.
</output>
